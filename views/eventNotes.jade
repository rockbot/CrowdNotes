title Create an Event!
p 
  a(href='/') HOME
div.container_12
  h2#header.grid_12 CrowdNotes - Review all notes from a specific event!
  div.clear

  div.grid_4
    form(method='post')
      div
        label(for='eventid') Name of Event:
        select(id='eventid', name='eventid')
          each event in currentEvents 
            option(value='#{event._id}')= event.name
      br
      div
        input(type='submit', value='Get the notes!')
  div.clear
