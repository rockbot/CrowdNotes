h1= title
p Here are the notes you requested:
ul
  each note in notesList
    li User: #{note._user.name.first} #{note._user.name.last} 
      p Body: #{note.body}
      p Date: #{note.date.toLocaleDateString()}
      p Time: #{note.date.toLocaleTimeString()}
      p Event: #{note._event.name}
